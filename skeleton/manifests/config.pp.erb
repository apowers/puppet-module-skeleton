# == Class: <%= metadata.name %>
#
# Full description of class <%= metadata.name %> is in the README.
#
class <%= metadata.name %>::config (
    $ensure   = $<%= metadata.name %>::config_ensure,
    $options  = $<%= metadata.name %>::config_options,
) {
  include <%= metadata.name %>::params
  file { $<%= metadata.name %>::params::config_file:
    ensure  => $ensure,
    mode    => '0440',
    content => template('<%= metadata.name %>/<%= metadata.name %>.conf.erb')
  }
}

