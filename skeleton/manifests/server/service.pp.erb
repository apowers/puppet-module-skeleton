# == Class: <%= metadata.name %>
#
# Full description of class <%= metadata.name %> is in the README.
#
class <%= metadata.name %>::service (
  $ensure       = $<%= metadata.name %>::server::service_ensure,
  $enable       = $<%= metadata.name %>::server::service_enable,
  $service_name = $<%= metadata.name %>::server::service_name,
) {
  service { $service_name:
    ensure => $ensure,
    enable => $enable,
  }
}
